export const tileLayerUrlTemplate = 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png'

export const tileLayerOptions = {
  attribution: '&copy; OpenStreetMap contributors',
  detectRetina: true,
  urlTemplate: tileLayerUrlTemplate
}
